name: movenet_data
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  camera: ^0.12.1
  flutter:
    sdk: flutter
  google_mlkit_pose_detection: ^0.16.1
  movenet_domain: ^1.0.0
  path_provider: ^2.1.5
  shared_preferences: ^2.5.4

flutter:
  assets:
    - assets/kinetics_600_labels.json
